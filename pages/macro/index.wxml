<l-grid l-class="grid" show-border="{{true}}">
    <l-grid-item
            wx:for="{{toolList}}" key="{{index}}" slot="{{index}}" bind:linitemtap="gotoPage">
        <l-icon name="{{item.icon_name}}" />
        <view class="text">{{item.name}}</view>
    </l-grid-item>
</l-grid>
